buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.0'
    }
}

// 应用于所有子项目的公共配置
allprojects {
    tasks.withType(JavaCompile) {
        options.encoding = 'UTF-8'
    }
} 